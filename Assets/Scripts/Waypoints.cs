using UnityEngine;
using System.Collections;

public class Waypoints : MonoBehaviour {

    /*public static Transform[] waypoints;

    public GameObject waypoint;
    public GameObject startNode;
    public GameObject endNode;

    void start()
        {
            GameObject startWaypoint = (GameObject)Instantiate(waypoint, startNode.transform.position, startNode.transform.rotation);
            waypoints[0] = transform.GetChild(0);
        }
    void makeWay(Node node)
    {
        waypoints = new Transform[transform.childCount];
        for (int i = 1; i < node.wayLenght; i++)
            {
                GameObject way = (GameObject)Instantiate(waypoint, node.transform.position, node.transform.rotation);
                
            }
        }
    void Awake()
        {
        waypoints = new Transform[transform.childCount];
        for (int i = 0; i < waypoints.Length; i++)
        {
            waypoints[i] = transform.GetChild(i);
        }
    }
	void Update () {
        
	}*/
}
